DATABASE_URL="mongodb+srv://USER:PASS@HOST/DATABASE?retryWrites=true&w=majority"

# For local Docker MongoDB usage with replica set (no auth):
# MONGODB_PORT=27017
# DATABASE_URL="mongodb://localhost:${MONGODB_PORT}/ecom?replicaSet=rs0"

# Enable mock data to run without a DB
# MOCK_DATA=1
